# 📊 代码对比分析：优化版 vs V3.0.0

**分析时间**：2026-03-06 11:09

---

## 🎯 核心改进对比

| 改进项 | 优化版 | V3.0.0 | 优势方 |
|--------|--------|--------|--------|
| **API 安全** | st.secrets + fallback | 硬编码 | ✅ 优化版 |
| **JSON 输出** | response_format 强制 | 文本解析 | ✅ 优化版 |
| **代码结构** | 简洁清晰 | 较复杂 | ✅ 优化版 |
| **系统 Prompt** | 精简 | 完整详细 | 🔄 看需求 |
| **UI 界面** | 简洁实用 | 功能丰富 | 🔄 看需求 |
| **功能完整性** | 基础 | 完整 | ✅ V3.0.0 |

---

## 🔍 详细对比

### 1. API 安全配置

#### 优化版
```python
# ✅ 优先使用 st.secrets
try:
    API_KEY = st.secrets["DEEPSEEK_API_KEY"]
except (KeyError, FileNotFoundError):
    # 仅作为 fallback
    API_KEY = "sk-62a693ae5cb24574bd9df2a9bb53cd99"
    st.sidebar.warning("⚠️ 正在使用硬编码的 API Key")
```

**优点**：
- ✅ 安全性高
- ✅ 生产环境友好
- ✅ 有安全警告

#### V3.0.0
```python
DEEPSEEK_API_KEY = "sk-62a693ae5cb24574bd9df2a9bb53cd99"
```

**优点**：
- ✅ 简单直接
- ⚠️ 安全性较低

---

### 2. JSON 输出控制

#### 优化版
```python
# ✅ 强制 JSON 输出
response_format={"type": "json_object"}

# ✅ 直接解析 JSON
result_json = json.loads(result_content)
```

**优点**：
- ✅ 稳定性高
- ✅ 解析简单
- ✅ 错误处理清晰

#### V3.0.0
```python
# ⚠️ 文本格式输出，需要解析
# 手动提取 JSON 部分
```

**优点**：
- ⚠️ 需要复杂的解析逻辑
- ⚠️ 容易出错

---

### 3. 系统 Prompt

#### 优化版
```python
"""你是一位顶级的电影导演、影视工业专家和 AI 视频专家。

## 你的角色
1. **视觉导演** - 负责镜头语言、运镜设计、光影构图
2. **情感分析师** - 负责识别故事的情绪转折点和冲突爆发点
3. **分镜编剧** - 负责将故事拆分为逻辑连贯的导演级分镜

## 你的任务
将用户的文字稿转化为具有**空间逻辑一致性**和**前后分镜连续**的 Seedance 2.0 专业分镜。

## 三层导演逻辑
### 第一层：全局逻辑层（一致性保证）
必须先提取并锁定：核心角色设定、环境设定、光影基调。
### 第二层：导演编剧层（标准化剧本）
生成 3-5 个逻辑连贯的分镜。包含：镜号、场景、运镜、动态画面描述、旁白、音效。
### 第三层：Seedance 2.0 适配层
将场景转化为高张力、细致表情和物理动态的动态描述。
"""
```

**优点**：
- ✅ 精简清晰
- ✅ 易于理解

#### V3.0.0
```python
"""你是一位顶级的电影导演、影视工业专家和 AI 视频专家。

## 你的角色
1. **视觉导演** - 负责镜头语言、运镜设计、光影构图
2. **情感分析师** - 负责识别故事的情绪转折点和冲突爆发点
3. **分镜编剧** - 负责将故事拆分为逻辑连贯的导演级分镜

## 你的任务
将用户的文字稿转化为具有**空间逻辑一致性**和**前后分镜连续**的 Seedance 2.0 专业分镜。

## 三层导演逻辑

### 第一层：全局逻辑层（一致性保证）
在生成任何分镜之前，必须先提取并锁定以下全局设定：
1. **核心角色设定**：
   - 外貌特征（发型、发色、面部特征）
   - 服装风格（颜色、材质、风格）
   - 性格特点（冷酷、温柔、坚毅等）

2. **环境设定**：
   - 空间位置（室内/室外、具体地点）
   - 时间（清晨、黄昏、夜晚、深夜）
   - 天气（晴天、雨天、雪天、暴风雨）

3. **光影基调**：
   - 整体色调（冷/暖）
   - 对比度（高/低）
   - 光效风格（自然/戏剧）

**重要性**：这些设定在所有分镜中必须保持一致，防止"角色变脸"或"环境跳戏"。

### 第二层：导演编剧层（标准化剧本）
根据故事的情绪逻辑，生成 3-5 个逻辑连贯的分镜。每个分镜必须包含以下 6 个标准化要素：

1. **镜号 (Shot ID)**：明确序列顺序（如：Shot 01, Shot 02）
2. **场景 (Setting)**：定义空间位置与时间（如：破旧小酒馆 - 深夜）
3. **运镜 (Camera)**：专业摄影机语言（如：低角度推近特写、荷兰斜角、希区柯克变焦）
4. **动态画面描述提示词 (Visual Prompt)**：核心输出，包含：
   - 电影级画质标签（8K, 超高清）
   - 主体描述（外貌 + 服装，必须一致）
   - 表情微操（瞳孔微动、汗水滑落、肌肉颤抖等）
   - 爆发性动作（慢动作爆发、碎屑飞溅、衣物撕裂、剧烈喘息等）
   - 环境特效（水花溅射、烟雾扩散、光效闪烁）
   - 运镜描述（推、拉、摇、移、跟、环绕、变焦等）
5. **旁白 (VO)**：叙事台词或内心独白
6. **音效 (SFX)**：环境音、动作音、情绪转场音（如：暴雨击打窗户声、沉重的拔刀声）

### 第三层：Seedance 2.0 适配层
将场景转化为高张力、细致表情和物理动态的动态描述，并添加 Seedance 2.0 参数。

## 空间连续性要求
- 必须保持同一场景内的方位逻辑一致
- 如果 Shot 1 角色在左侧，Shot 2 的转场必须符合视觉逻辑
- 角色的位置、朝向、动作必须连续，不能跳跃

## 角色一致性要求
- 在所有分镜提示词中，必须使用相同的外貌特征描述
- 服装、发型、面部特征必须保持一致
- 避免使用"然后"、"接着"等简单的过渡词
- 使用具有画面感的连续性描述

## Seedance 2.0 优化要求
- **细致表情**：必须包含（如：瞳孔收缩、嘴角抽搐、泪水盈眶、咬紧牙关）
- **高张力动作**：必须包含（如：肌肉发力、衣物随风摆动、物理碰撞效果）
- **物理交互**：必须包含（如：水花溅射、烟雾扩散、粒子特效）
"""
```

**优点**：
- ✅ 详细完整
- ✅ 指导性强
- ✅ 包含所有细节

---

### 4. UI 界面

#### 优化版
```python
def render_ai_director():
    st.subheader("🎬 Seedance 2.0 AI 视觉导演系统")

    api_mode = st.toggle("🤖 使用 DeepSeek-V3 API", value=True)

    col1, col2 = st.columns([2, 1])
    with col1:
        story_text = st.text_area("📖 1. 故事/脚本输入", height=120)
    with col2:
        st.info("💡 系统会自动分析：情绪转折点、冲突爆发点")

    st.divider()

    # 简洁的参数选择
    st.subheader("🎛️ 2. 导演控制参数")
    col1, col2, col3 = st.columns(3)
    with col1:
        tension_level = st.selectbox("张力等级", ["低 (日常)", "中 (剧情)", "高 (动作/爆发)"])
    with col2:
        visual_style = st.selectbox("视觉风格", ["赛博朋克", "唯美日系", "暗黑废土", "王家卫质感"])
    with col3:
        motion_param = st.slider("画面动态程度 (--motion)", 1, 10, 5)
```

**优点**：
- ✅ 简洁直观
- ✅ 快速上手
- ✅ 参数集中

#### V3.0.0
```python
def render_ai_director():
    st.subheader("🎬 Seedance 2.0 AI 视觉导演系统")
    st.caption("通过三层导演逻辑，生成极致张力、细腻表情和戏剧化冲突的全中文分镜提示词")

    api_mode = st.toggle("🤖 使用 DeepSeek-V3 API", value=False)

    col1, col2 = st.columns([2, 1])

    with col1:
        story_text = st.text_area(
            "📖 1. 故事/脚本输入",
            placeholder="输入100字左右的故事或场景描述...",
            height=120
        )

    with col2:
        st.info("💡 输入提示")
        st.markdown("""
        - 系统会自动识别：
          • 情绪转折点
          • 冲突爆发点
          • 情绪高潮点
        - 推荐长度：50-150字
        """)

    st.divider()

    # 全局设定输入
    col1, col2, col3, col4 = st.columns(4)
    with col1:
        global_character = st.text_input("全局角色设定")
    with col2:
        global_environment = st.text_input("全局环境设定")
    with col3:
        global_lighting = st.text_input("全局光影基调")
    with col4:
        global_style = st.text_input("全局视觉风格")

    st.divider()

    # 详细的导演控制参数
    col1, col2, col3 = st.columns(3)

    with col1:
        tension_level = st.select_slider(
            "🎭 2. 张力强度调节器",
            options=list(TENSION_MOTION_MAP.keys()),
            value="明显起伏 (中等)"
        )

        emotion_filter = st.multiselect(
            "🎨 3. 情绪滤镜",
            list(EMOTION_FILTERS.keys())
        )

    with col2:
        expression_select = st.multiselect(
            "😐 4. 表情微操（可选）",
            list(EXPRESSION_ENHANCEMENTS.keys())
        )

        action_select = st.multiselect(
            "🏃 5. 动作张力（可选）",
            list(ACTION_ENHANCEMENTS.keys())
        )

    with col3:
        camera_select = st.multiselect(
            "🎥 6. 大师运镜（可选）",
            list(MASTER_CAMERA_MOVES.keys())
        )

        lighting_select = st.multiselect(
            "💡 7. 戏剧化光影（可选）",
            list(DRAMATIC_LIGHTING.keys())
        )

    aspect_ratio = st.selectbox(
        "📐 画面比例",
        ["16:9 横屏", "9:16 竖屏", "1:1 方形", "2.35:1 电影宽屏"],
        index=0
    )
```

**优点**：
- ✅ 功能丰富
- ✅ 参数详细
- ✅ 专业性强

---

### 5. 代码结构

#### 优化版
```
1. API 安全配置
2. 核心系统提示词
3. AI 调用与解析
4. UI 渲染主函数
5. 主函数入口
```

**优点**：
- ✅ 结构清晰
- ✅ 易于维护
- ✅ 代码简洁

#### V3.0.0
```
1. 导入和配置
2. 导演系统 Prompt
3. DeepSeek/OpenAI API 配置
4. 张力/情绪/表情/动作/运镜/光影库
5. 辅助函数
6. 核心函数
7. UI 渲染函数
8. 主函数
```

**优点**：
- ✅ 功能完整
- ✅ 模块化好
- ⚠️ 代码量大

---

## 💡 整合建议

### 方案一：创建 V3.0.2（推荐）

**目标**：结合两者的优点

**整合内容**：
1. ✅ 采用优化版的 API 安全配置
2. ✅ 采用优化版的 JSON 强制输出
3. ✅ 保留 V3.0.0 的详细系统 Prompt
4. ✅ 保留 V3.0.0 的丰富 UI 功能
5. ✅ 保留 V3.0.0 的所有功能模块

**文件结构**：
```
app_v3.0.2.py - 主应用（整合版）
api_config.py - API 配置管理
```

---

### 方案二：保留两个版本

**V3.0.0**：
- 完整功能
- 专业用户
- 详细参数控制

**优化版（V3.0.2-Lite）**：
- 简洁快速
- 普通用户
- 快速生成

---

### 方案三：UI 切换模式

在 V3.0.0 中添加"简洁模式"和"专业模式"切换：

**简洁模式**：
- 使用优化版的简化 UI
- 基础参数

**专业模式**：
- 使用 V3.0.0 的完整 UI
- 所有参数

---

## 🎯 推荐方案

### 短期：创建 V3.0.2-Lite

1. 创建 `app_v3.0.2_lite.py`
2. 整合优化版的代码
3. 优化 API 安全配置
4. 使用 JSON 强制输出
5. 保留三层导演逻辑

### 长期：统一到 V3.0.2

1. 整合两个版本的优点
2. 添加模式切换（简洁/专业）
3. 优化代码结构
4. 改进 API 安全性

---

## 📊 代码对比总结

| 方面 | 优化版 | V3.0.0 | 整合版（V3.0.2） |
|------|--------|--------|------------------|
| **API 安全** | ✅ 高 | ⚠️ 中 | ✅ 高 |
| **JSON 输出** | ✅ 强制 | ⚠️ 解析 | ✅ 强制 |
| **系统 Prompt** | ✅ 精简 | ✅ 详细 | ✅ 可选 |
| **UI 界面** | ✅ 简洁 | ✅ 丰富 | ✅ 可选 |
| **功能完整性** | ⚠️ 基础 | ✅ 完整 | ✅ 完整 |
| **代码结构** | ✅ 清晰 | ✅ 模块化 | ✅ 优化 |
| **适用场景** | 快速 | 专业 | 通用 |

---

**需要我帮你创建整合版本吗？**
